'use client'

import { useLanguage } from '../contexts/LanguageContext'
import AdvancedLanguageDropdown from '../components/language/LanguageDropdown'

export default function MainPage() {
  const { t } = useLanguage()

  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-600 via-purple-700 to-blue-800">
      {/* Header */}
      <header className="flex justify-between items-center p-6">
        <div className="flex items-center space-x-4">
          <div className="w-12 h-12 bg-white rounded-xl flex items-center justify-center font-bold text-purple-700">
            M4C
          </div>
          <div>
            <h1 className="text-white font-bold text-xl">{t('main_title')}</h1>
            <p className="text-purple-200 text-sm">{t('website')} • {t('world_leader')}</p>
          </div>
        </div>
        
        <div className="flex items-center space-x-4">
          <div className="text-purple-200 text-sm">
            👨‍👩‍👧‍👦 {t('families_trust')}
          </div>
          <AdvancedLanguageDropdown />
        </div>
      </header>

      {/* Main Content */}
      <main className="container mx-auto px-6 py-12 text-center">
        {/* Badge */}
        <div className="inline-flex items-center bg-green-100 text-green-800 px-4 py-2 rounded-full text-sm font-medium mb-8">
          📱 {t('app_educative')}
        </div>

        {/* Title */}
        <h1 className="text-6xl font-bold text-white mb-6">
          {t('main_title')}
        </h1>
        
        {/* Subtitle */}
        <p className="text-2xl text-purple-200 mb-4 max-w-4xl mx-auto">
          {t('main_subtitle')}
        </p>
        
        {/* Join families */}
        <p className="text-xl text-green-300 mb-12">
          {t('join_families')}
        </p>

        {/* Action Buttons */}
        <div className="flex justify-center space-x-6 mb-16">
          <button className="bg-green-500 hover:bg-green-600 text-white px-8 py-4 rounded-2xl font-bold text-lg flex items-center space-x-2 transition-all">
            <span>🎁</span>
            <span>{t('start_free')}</span>
            <span className="text-sm bg-green-600 px-2 py-1 rounded-full">{t('trial_14d')}</span>
          </button>
          
          <button className="bg-purple-500 hover:bg-purple-600 text-white px-8 py-4 rounded-2xl font-bold text-lg flex items-center space-x-2 transition-all">
            <span>📊</span>
            <span>{t('compare_prices')}</span>
          </button>
        </div>

        {/* Pricing Cards */}
        <div className="grid grid-cols-1 md:grid-cols-4 gap-6 max-w-7xl mx-auto">
          {/* Free Plan */}
          <div className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 text-white">
            <h3 className="text-2xl font-bold mb-4">{t('plan_free')}</h3>
            <div className="space-y-3 mb-6">
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('community_support')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('offline_limited')}</span>
              </div>
            </div>
            <button className="w-full bg-gray-600 hover:bg-gray-700 text-white py-3 rounded-xl font-bold transition-all">
              {t('start_free')}
            </button>
          </div>

          {/* 14-day Trial */}
          <div className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 text-white">
            <h3 className="text-2xl font-bold mb-4">{t('plan_trial_14')}</h3>
            <div className="space-y-3 mb-6">
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('unlimited_questions')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('complete_levels')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('child_profiles_5')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('languages_30_complete')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('offline_total')}</span>
              </div>
            </div>
            <button className="w-full bg-blue-600 hover:bg-blue-700 text-white py-3 rounded-xl font-bold transition-all">
              {t('free_trial_14')}
            </button>
          </div>

          {/* 7-day Trial */}
          <div className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 text-white">
            <h3 className="text-2xl font-bold mb-4">{t('plan_trial_7')}</h3>
            <div className="space-y-3 mb-6">
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('unlimited_questions')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('complete_levels')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('child_profiles_2')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('languages_30')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('offline_mode')}</span>
              </div>
            </div>
            <button className="w-full bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-xl font-bold transition-all">
              {t('free_trial_7')}
            </button>
          </div>

          {/* Family Plan */}
          <div className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 text-white">
            <h3 className="text-2xl font-bold mb-4">{t('plan_family')}</h3>
            <div className="space-y-3 mb-6">
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('family_plan_all')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('student_profiles_30')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('teacher_dashboard')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('homework_assignment')}</span>
              </div>
              <div className="flex items-center space-x-2">
                <span className="text-green-400">✓</span>
                <span className="text-sm">{t('detailed_reports')}</span>
              </div>
            </div>
            <button className="w-full bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white py-3 rounded-xl font-bold transition-all">
              {t('start_free_btn')}
            </button>
          </div>
        </div>
      </main>
    </div>
  )
}
